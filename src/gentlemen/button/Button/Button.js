const Button = ({ selectAll, action }) => {
  return (
    <section className="controls">
      <p className="info">0 gentlemen pointing at you</p>
      <button className="button button--select">Select all</button>
    </section>
  );
};
export default Button;
